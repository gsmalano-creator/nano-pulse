# This project is built by Cloudflare Workers Builds, which has no credentials for
# any private registry. Resolve everything from public npm so package-lock.json
# stays installable in CI.
registry=https://registry.npmjs.org/
